<Window x:Class="TheLibrary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="The Library" Width="1420" Height="920" MinWidth="760" MinHeight="560"
        WindowStartupLocation="CenterScreen" Background="#120F0D" Foreground="#EFE1BE"
        FontFamily="Segoe UI" UseLayoutRounding="True">
  <Window.Resources>
    <SolidColorBrush x:Key="Ink" Color="#120F0D" />
    <SolidColorBrush x:Key="Leather" Color="#221E1A" />
    <SolidColorBrush x:Key="RaisedLeather" Color="#302A23" />
    <SolidColorBrush x:Key="Brass" Color="#B58F43" />
    <SolidColorBrush x:Key="DarkBrass" Color="#534123" />
    <SolidColorBrush x:Key="Ivory" Color="#EFE1BE" />
    <SolidColorBrush x:Key="Muted" Color="#B5A689" />
    <DropShadowEffect x:Key="SoftShadow" BlurRadius="16" ShadowDepth="3" Opacity="0.45" Color="#000000" />

    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource Ivory}" />
    </Style>
    <Style TargetType="Label">
      <Setter Property="Foreground" Value="{StaticResource Muted}" />
      <Setter Property="Padding" Value="0,4" />
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Background" Value="#181512" />
      <Setter Property="Foreground" Value="{StaticResource Ivory}" />
      <Setter Property="CaretBrush" Value="{StaticResource Brass}" />
      <Setter Property="BorderBrush" Value="#6B5730" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="10,7" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <Style TargetType="ComboBox">
      <Setter Property="Background" Value="#181512" />
      <Setter Property="Foreground" Value="#16120F" />
      <Setter Property="BorderBrush" Value="#6B5730" />
      <Setter Property="Padding" Value="8,6" />
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="{StaticResource Muted}" />
      <Setter Property="Margin" Value="0,6" />
    </Style>
    <Style x:Key="ArchiveButton" TargetType="Button">
      <Setter Property="Background" Value="{StaticResource RaisedLeather}" />
      <Setter Property="Foreground" Value="{StaticResource Ivory}" />
      <Setter Property="BorderBrush" Value="{StaticResource Brass}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="14,9" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#534123" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#B58F43" />
                <Setter Property="Foreground" Value="#120F0D" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.45" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="NavButton" TargetType="Button" BasedOn="{StaticResource ArchiveButton}">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0,0,0,1" />
      <Setter Property="BorderBrush" Value="#3E3427" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="Padding" Value="20,13" />
      <Setter Property="FontWeight" Value="Normal" />
    </Style>
    <Style x:Key="Heading" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Georgia" />
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="Foreground" Value="{StaticResource Ivory}" />
    </Style>
    <Style x:Key="SectionCard" TargetType="Border">
      <Setter Property="Background" Value="{StaticResource Leather}" />
      <Setter Property="BorderBrush" Value="#59492A" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="18" />
      <Setter Property="CornerRadius" Value="3" />
      <Setter Property="Effect" Value="{StaticResource SoftShadow}" />
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="210" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Border Grid.Column="0" Background="#191613" BorderBrush="#6A552D" BorderThickness="0,0,1,0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="92" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Margin="14" BorderBrush="{StaticResource Brass}" BorderThickness="1" Padding="10">
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="THE LIBRARY" Style="{StaticResource Heading}" FontSize="19" HorizontalAlignment="Center" />
            <TextBlock Text="PRIVATE COVER ARCHIVE" Foreground="{StaticResource Brass}" FontSize="9" HorizontalAlignment="Center" Margin="0,4,0,0" />
          </StackPanel>
        </Border>
        <StackPanel Grid.Row="1" Margin="0,8">
          <Button Content="All Covers" Style="{StaticResource NavButton}" Click="AllCovers_Click" />
          <Button Content="Fandoms" Style="{StaticResource NavButton}" Click="Fandoms_Click" />
          <Button Content="Ships" Style="{StaticResource NavButton}" Click="Ships_Click" />
          <Button Content="Complete Sets" Style="{StaticResource NavButton}" Click="CompleteSets_Click" />
          <Button Content="Incomplete Sets" Style="{StaticResource NavButton}" Click="IncompleteSets_Click" />
          <Button Content="Archive" Style="{StaticResource NavButton}" Click="Archived_Click" />
          <Separator Margin="18,14" Background="#6A552D" />
          <Button Content="Add &amp; Edit Covers" Style="{StaticResource NavButton}" Click="AddCovers_Click" />
          <Button Content="Settings &amp; Backup" Style="{StaticResource NavButton}" Click="Settings_Click" />
        </StackPanel>
        <TextBlock Grid.Row="2" x:Name="SidebarStats" Foreground="{StaticResource Muted}" FontSize="11" Margin="18" TextWrapping="Wrap" />
      </Grid>
    </Border>

    <Grid Grid.Column="1">
      <!-- ARCHIVE -->
      <Grid x:Name="ArchivePage">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Background="#1B1714" BorderBrush="#4C3D25" BorderThickness="0,0,0,1" Padding="24,18">
          <Grid>
            <Grid.RowDefinitions><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <DockPanel>
              <StackPanel DockPanel.Dock="Left">
                <TextBlock x:Name="ArchiveHeading" Text="Cover Archive" Style="{StaticResource Heading}" FontSize="27" />
                <TextBlock x:Name="ArchiveSubheading" Text="Grouped Sets" Foreground="{StaticResource Brass}" Margin="0,4,0,0" />
              </StackPanel>
              <TextBlock x:Name="ResultCount" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Bottom" Foreground="{StaticResource Muted}" />
            </DockPanel>
            <Grid Grid.Row="1" Margin="0,16,0,0">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/><ColumnDefinition Width="150"/><ColumnDefinition Width="150"/><ColumnDefinition Width="150"/>
              </Grid.ColumnDefinitions>
              <TextBox x:Name="SearchBox" Grid.Column="0" Margin="0,0,10,0" ToolTip="Search title, fandom, ship, tags, or filename" TextChanged="ArchiveFilter_Changed" />
              <ComboBox x:Name="PositionFilter" Grid.Column="1" Margin="0,0,10,0" SelectionChanged="ArchiveFilter_Changed">
                <ComboBoxItem Content="All Panels" IsSelected="True"/><ComboBoxItem Content="Front Cover"/><ComboBoxItem Content="Spine"/><ComboBoxItem Content="Back Cover"/><ComboBoxItem Content="Other"/>
              </ComboBox>
              <ComboBox x:Name="SortBox" Grid.Column="2" Margin="0,0,10,0" SelectionChanged="ArchiveFilter_Changed">
                <ComboBoxItem Content="Recently Added" IsSelected="True"/><ComboBoxItem Content="Title"/><ComboBoxItem Content="Fandom"/><ComboBoxItem Content="Ship"/>
              </ComboBox>
              <ComboBox x:Name="ViewMode" Grid.Column="3" SelectionChanged="ArchiveFilter_Changed">
                <ComboBoxItem Content="Grouped Sets" IsSelected="True"/><ComboBoxItem Content="All Images"/>
              </ComboBox>
            </Grid>
          </Grid>
        </Border>

        <Grid Grid.Row="1">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition x:Name="DetailsColumn" Width="350"/></Grid.ColumnDefinitions>
          <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Padding="14">
            <Grid>
              <ItemsControl x:Name="CoverItems" ItemsSource="{Binding Cards}">
                <ItemsControl.ItemsPanel>
                  <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Button Style="{StaticResource ArchiveButton}" Padding="0" Margin="9" Width="190" Height="315"
                            Tag="{Binding}" Click="CoverCard_Click" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                      <Border Background="#221E1A" BorderBrush="#6A552D" BorderThickness="1" Padding="10">
                        <Grid>
                          <Grid.RowDefinitions><RowDefinition Height="210"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                          <Border Background="#0E0C0B" BorderBrush="#4C3D25" BorderThickness="1">
                            <Image Source="{Binding CoverImage}" Stretch="Uniform" />
                          </Border>
                          <StackPanel Grid.Row="1" Margin="2,10,2,0">
                            <TextBlock Text="{Binding Title}" Style="{StaticResource Heading}" FontSize="14" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Fandom}" Foreground="{StaticResource Muted}" Margin="0,5,0,0" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Ship}" Foreground="{StaticResource Muted}" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding PanelSummary}" Foreground="{StaticResource Brass}" FontSize="10" Margin="0,5,0,0" />
                          </StackPanel>
                        </Grid>
                      </Border>
                    </Button>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
              <StackPanel x:Name="ArchiveEmptyState" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="40">
                <TextBlock Text="No covers found" Style="{StaticResource Heading}" FontSize="24" HorizontalAlignment="Center" />
                <TextBlock Text="Try another search or add a cover to the archive." Foreground="{StaticResource Muted}" Margin="0,8" />
                <Button Content="ADD A COVER" Style="{StaticResource ArchiveButton}" Margin="40,12" Click="AddCovers_Click" />
              </StackPanel>
            </Grid>
          </ScrollViewer>

          <Border Grid.Column="1" Background="#1A1714" BorderBrush="#6A552D" BorderThickness="1,0,0,0" Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
              <StackPanel>
                <TextBlock Text="ARCHIVE RECORD" Foreground="{StaticResource Brass}" FontSize="10" />
                <Border Background="#0E0C0B" BorderBrush="#6A552D" BorderThickness="1" Height="230" Margin="0,12">
                  <Image x:Name="DetailImage" Stretch="Uniform" />
                </Border>
                <TextBlock x:Name="DetailEmpty" Text="Select a cover or set to inspect it." Foreground="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,8" />
                <StackPanel x:Name="DetailEditor" IsEnabled="False">
                  <Label Content="Title / Book"/><TextBox x:Name="DetailProject" />
                  <Label Content="Fandom" Margin="0,7,0,0"/><TextBox x:Name="DetailFandom" />
                  <Label Content="Ship" Margin="0,7,0,0"/><TextBox x:Name="DetailShip" />
                  <Label Content="Tags" Margin="0,7,0,0"/><TextBox x:Name="DetailTags" />
                  <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,5,0"><Label Content="Panel"/><ComboBox x:Name="DetailPosition"><ComboBoxItem Content="Front Cover"/><ComboBoxItem Content="Spine"/><ComboBoxItem Content="Back Cover"/><ComboBoxItem Content="Other"/></ComboBox></StackPanel>
                    <StackPanel Grid.Column="1" Margin="5,0,0,0"><Label Content="Type"/><ComboBox x:Name="DetailType"><ComboBoxItem Content="Original"/><ComboBoxItem Content="FanFiction"/><ComboBoxItem Content="Unsorted"/></ComboBox></StackPanel>
                  </Grid>
                  <TextBlock x:Name="DetailPieces" Foreground="{StaticResource Brass}" Margin="0,12" TextWrapping="Wrap" />
                  <Button Content="SAVE CHANGES" Style="{StaticResource ArchiveButton}" Margin="0,6" Click="SaveDetails_Click" />
                  <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <Button Content="DOWNLOAD" Style="{StaticResource ArchiveButton}" Margin="0,4,4,4" Click="Download_Click"/>
                    <Button Grid.Column="1" Content="REPLACE" Style="{StaticResource ArchiveButton}" Margin="4" Click="Replace_Click"/>
                  </Grid>
                  <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <Button x:Name="ArchiveButton" Content="ARCHIVE" Style="{StaticResource ArchiveButton}" Margin="0,4,4,4" Click="ArchiveSelected_Click"/>
                    <Button Grid.Column="1" Content="DELETE" Style="{StaticResource ArchiveButton}" Margin="4" Click="DeleteSelected_Click"/>
                  </Grid>
                  <TextBlock x:Name="AutosaveStatus" Foreground="{StaticResource Muted}" FontSize="10" Margin="0,8" HorizontalAlignment="Right" />
                </StackPanel>
              </StackPanel>
            </ScrollViewer>
          </Border>
        </Grid>
      </Grid>

      <!-- ADD AND EDIT -->
      <Grid x:Name="AddPage" Visibility="Collapsed">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Background="#1B1714" BorderBrush="#4C3D25" BorderThickness="0,0,0,1" Padding="26,20">
          <DockPanel>
            <StackPanel DockPanel.Dock="Left"><TextBlock Text="Add &amp; Edit Covers" Style="{StaticResource Heading}" FontSize="27"/><TextBlock Text="Import a panel or split full cover wraps" Foreground="{StaticResource Brass}" Margin="0,4"/></StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
              <Button Content="SINGLE PANEL" Style="{StaticResource ArchiveButton}" Margin="4" Click="SingleMode_Click"/>
              <Button Content="FULL COVER WRAP" Style="{StaticResource ArchiveButton}" Margin="4" Click="WrapMode_Click"/>
            </StackPanel>
          </DockPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="26">
          <Grid>
            <Grid x:Name="SinglePanel">
              <Grid.ColumnDefinitions><ColumnDefinition Width="420"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
              <Border Style="{StaticResource SectionCard}" Margin="0,0,18,0">
                <StackPanel>
                  <TextBlock Text="Single Panel" Style="{StaticResource Heading}" FontSize="22" />
                  <TextBlock Text="Choose a separate front, spine, back, or other cover image." Foreground="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,6,0,16"/>
                  <Button Content="CHOOSE IMAGE" Style="{StaticResource ArchiveButton}" Click="ChooseSingle_Click" />
                  <TextBlock x:Name="SingleFileName" Text="No image selected" Foreground="{StaticResource Brass}" Margin="0,10" TextWrapping="Wrap" />
                  <Label Content="Panel"/><ComboBox x:Name="SinglePosition"><ComboBoxItem Content="Front Cover" IsSelected="True"/><ComboBoxItem Content="Spine"/><ComboBoxItem Content="Back Cover"/><ComboBoxItem Content="Other"/></ComboBox>
                  <Label Content="Cover Type" Margin="0,7,0,0"/><ComboBox x:Name="SingleType"><ComboBoxItem Content="Original"/><ComboBoxItem Content="FanFiction" IsSelected="True"/><ComboBoxItem Content="Unsorted"/></ComboBox>
                  <Label Content="Title / Book" Margin="0,7,0,0"/><TextBox x:Name="SingleProject" />
                  <Label Content="Fandom" Margin="0,7,0,0"/><TextBox x:Name="SingleFandom" />
                  <Label Content="Ship" Margin="0,7,0,0"/><TextBox x:Name="SingleShip" />
                  <Label Content="Tags" Margin="0,7,0,0"/><TextBox x:Name="SingleTags" />
                  <CheckBox x:Name="SingleRemoveSource" Content="Remove the outside source after it is safely imported" IsChecked="True" />
                  <Button Content="SAVE PANEL" Style="{StaticResource ArchiveButton}" Margin="0,12,0,0" Click="SaveSingle_Click" />
                  <TextBlock x:Name="SingleStatus" Foreground="{StaticResource Brass}" Margin="0,10" TextWrapping="Wrap" />
                </StackPanel>
              </Border>
              <Border Grid.Column="1" Style="{StaticResource SectionCard}">
                <Grid><Image x:Name="SinglePreview" Stretch="Uniform"/><TextBlock x:Name="SinglePreviewEmpty" Text="Selected image preview" Foreground="{StaticResource Muted}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Grid>
              </Border>
            </Grid>

            <Grid x:Name="WrapPanel" Visibility="Collapsed">
              <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
              <Border Style="{StaticResource SectionCard}" Margin="0,0,0,16">
                <DockPanel>
                  <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="Full Cover Wrap Queue" Style="{StaticResource Heading}" FontSize="22" />
                    <TextBlock Text="The wrap is only a source. The Library saves Back, Spine, and Front as separate panels." Foreground="{StaticResource Brass}" Margin="0,5" />
                  </StackPanel>
                  <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="ADD WRAP(S)" Style="{StaticResource ArchiveButton}" Margin="4" Click="ChooseWraps_Click"/>
                    <Button Content="SAVE ALL" Style="{StaticResource ArchiveButton}" Margin="4" Click="SaveAllWraps_Click"/>
                    <Button Content="CLEAR ALL" Style="{StaticResource ArchiveButton}" Margin="4" Click="ClearWraps_Click"/>
                  </StackPanel>
                </DockPanel>
              </Border>

              <Grid Grid.Row="1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="240"/><ColumnDefinition Width="*"/><ColumnDefinition Width="320"/></Grid.ColumnDefinitions>
                <Border Style="{StaticResource SectionCard}" Margin="0,0,14,0">
                  <StackPanel>
                    <TextBlock Text="QUEUE" Foreground="{StaticResource Brass}" FontSize="10" />
                    <ItemsControl x:Name="WrapQueue" ItemsSource="{Binding WrapJobs}">
                      <ItemsControl.ItemTemplate><DataTemplate><Button Style="{StaticResource NavButton}" Tag="{Binding}" Click="QueueJob_Click" Margin="0,4"><StackPanel><TextBlock Text="{Binding FileName}" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding Status}" Foreground="{StaticResource Brass}" FontSize="10"/></StackPanel></Button></DataTemplate></ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock x:Name="WrapQueueEmpty" Text="No full wraps queued." Foreground="{StaticResource Muted}" Margin="0,14" />
                  </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource SectionCard}" Margin="0,0,14,0">
                  <StackPanel>
                    <DockPanel><TextBlock x:Name="WrapEditorTitle" Text="Select a queued wrap" Style="{StaticResource Heading}" FontSize="18"/><StackPanel DockPanel.Dock="Right" Orientation="Horizontal"><Button Content="FIT" Style="{StaticResource ArchiveButton}" Padding="10,5" Margin="2" Click="FitZoom_Click"/><Button Content="100%" Style="{StaticResource ArchiveButton}" Padding="10,5" Margin="2" Click="ActualZoom_Click"/></StackPanel></DockPanel>
                    <ScrollViewer x:Name="WrapPreviewScroller" Height="390" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Margin="0,12" Background="#0D0B0A">
                      <Grid x:Name="WrapImageSurface" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Image x:Name="WrapSourcePreview" Stretch="Uniform" />
                        <Canvas x:Name="GuideCanvas" Background="Transparent" SizeChanged="GuideCanvas_SizeChanged">
                          <Thumb x:Name="LeftGuide" Width="10" Cursor="SizeWE" DragDelta="LeftGuide_DragDelta"><Thumb.Template><ControlTemplate><Border Background="#E0B753" Opacity="0.9"/></ControlTemplate></Thumb.Template></Thumb>
                          <Thumb x:Name="RightGuide" Width="10" Cursor="SizeWE" DragDelta="RightGuide_DragDelta"><Thumb.Template><ControlTemplate><Border Background="#E0B753" Opacity="0.9"/></ControlTemplate></Thumb.Template></Thumb>
                        </Canvas>
                      </Grid>
                    </ScrollViewer>
                    <TextBlock x:Name="GuideSummary" Foreground="{StaticResource Brass}" />
                    <Grid Margin="0,10,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                      <StackPanel Margin="0,0,6,0"><Label Content="Back / Spine edge"/><Slider x:Name="LeftSlider" Minimum="1" ValueChanged="GuideSlider_Changed"/><TextBox x:Name="LeftPixels" TextChanged="GuidePixels_Changed"/></StackPanel>
                      <StackPanel Grid.Column="1" Margin="6,0,0,0"><Label Content="Spine / Front edge"/><Slider x:Name="RightSlider" Minimum="2" ValueChanged="GuideSlider_Changed"/><TextBox x:Name="RightPixels" TextChanged="GuidePixels_Changed"/></StackPanel>
                    </Grid>
                    <TextBlock Text="LIVE PANEL PREVIEWS" Foreground="{StaticResource Brass}" FontSize="10" Margin="0,16,0,6" />
                    <Grid Height="190"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="100"/><ColumnDefinition/></Grid.ColumnDefinitions>
                      <Border Background="#0E0C0B" BorderBrush="#6A552D" BorderThickness="1" Margin="2"><Image x:Name="BackPreview" Stretch="Uniform"/></Border>
                      <Border Grid.Column="1" Background="#0E0C0B" BorderBrush="#6A552D" BorderThickness="1" Margin="2"><Image x:Name="SpinePreview" Stretch="Uniform"/></Border>
                      <Border Grid.Column="2" Background="#0E0C0B" BorderBrush="#6A552D" BorderThickness="1" Margin="2"><Image x:Name="FrontPreview" Stretch="Uniform"/></Border>
                    </Grid>
                  </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource SectionCard}">
                  <StackPanel>
                    <TextBlock Text="COVER RECORD" Foreground="{StaticResource Brass}" FontSize="10" />
                    <Label Content="Title / Book" Margin="0,8,0,0"/><TextBox x:Name="WrapProject" TextChanged="WrapMetadata_Changed" />
                    <Label Content="Fandom" Margin="0,7,0,0"/><TextBox x:Name="WrapFandom" TextChanged="WrapMetadata_Changed" />
                    <Label Content="Ship" Margin="0,7,0,0"/><TextBox x:Name="WrapShip" TextChanged="WrapMetadata_Changed" />
                    <Label Content="Tags" Margin="0,7,0,0"/><TextBox x:Name="WrapTags" TextChanged="WrapMetadata_Changed" />
                    <Label Content="Cover Type" Margin="0,7,0,0"/><ComboBox x:Name="WrapType" SelectionChanged="WrapMetadata_Changed"><ComboBoxItem Content="Original"/><ComboBoxItem Content="FanFiction" IsSelected="True"/><ComboBoxItem Content="Unsorted"/></ComboBox>
                    <CheckBox x:Name="WrapRemoveSource" Content="Remove the source wrap after saving its panels" IsChecked="True" />
                    <Button Content="SAVE" Style="{StaticResource ArchiveButton}" Margin="0,12,0,0" Click="SaveCurrentWrap_Click" />
                    <Button Content="REMOVE FROM QUEUE" Style="{StaticResource ArchiveButton}" Margin="0,8,0,0" Click="RemoveCurrentWrap_Click" />
                    <TextBlock x:Name="WrapStatus" Foreground="{StaticResource Brass}" TextWrapping="Wrap" Margin="0,10" />
                  </StackPanel>
                </Border>
              </Grid>
            </Grid>
          </Grid>
        </ScrollViewer>
      </Grid>

      <!-- SETTINGS -->
      <Grid x:Name="SettingsPage" Visibility="Collapsed">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Background="#1B1714" BorderBrush="#4C3D25" BorderThickness="0,0,0,1" Padding="26,20">
          <StackPanel><TextBlock Text="Settings &amp; Backup" Style="{StaticResource Heading}" FontSize="27"/><TextBlock Text="Storage, recovery, and application updates" Foreground="{StaticResource Brass}" Margin="0,4"/></StackPanel>
        </Border>
        <ScrollViewer Grid.Row="1" Padding="26" VerticalScrollBarVisibility="Auto">
          <WrapPanel>
            <Border Style="{StaticResource SectionCard}" Width="430" Margin="8">
              <StackPanel><TextBlock Text="Backup &amp; Restore" Style="{StaticResource Heading}" FontSize="20"/><TextBlock Text="Create one backup containing your metadata and every stored cover panel." Foreground="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,8,0,16"/><Button Content="EXPORT BACKUP" Style="{StaticResource ArchiveButton}" Margin="0,4" Click="ExportBackup_Click"/><Button Content="RESTORE BACKUP" Style="{StaticResource ArchiveButton}" Margin="0,4" Click="RestoreBackup_Click"/><TextBlock x:Name="BackupStatus" Foreground="{StaticResource Brass}" TextWrapping="Wrap" Margin="0,10"/></StackPanel>
            </Border>
            <Border Style="{StaticResource SectionCard}" Width="430" Margin="8">
              <StackPanel><TextBlock Text="Application Updates" Style="{StaticResource Heading}" FontSize="20"/><TextBlock x:Name="VersionText" Foreground="{StaticResource Muted}" Margin="0,8,0,16"/><Button Content="CHECK FOR UPDATES" Style="{StaticResource ArchiveButton}" Click="CheckUpdates_Click"/><TextBlock x:Name="UpdateStatus" Foreground="{StaticResource Brass}" TextWrapping="Wrap" Margin="0,10"/></StackPanel>
            </Border>
            <Border Style="{StaticResource SectionCard}" Width="430" Margin="8">
              <StackPanel><TextBlock Text="Private Storage" Style="{StaticResource Heading}" FontSize="20"/><TextBlock Text="The Library keeps your data separately from the application so updates cannot overwrite your covers." Foreground="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,8"/><TextBox x:Name="StoragePath" IsReadOnly="True" Margin="0,8"/><Button Content="OPEN STORAGE FOLDER" Style="{StaticResource ArchiveButton}" Click="OpenStorage_Click"/></StackPanel>
            </Border>
          </WrapPanel>
        </ScrollViewer>
      </Grid>
    </Grid>
  </Grid>
</Window>
