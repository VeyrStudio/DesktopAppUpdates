name: The Files Timeline v0.2.22
on:
  push:
    paths:
      - 'tools/the-files-timeline-v0222.py'
      - '.github/workflows/the-files-timeline-v0222.yml'
permissions:
  contents: write
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - name: Patch builder to exact current renderer hook
        shell: python
        run: |
          from pathlib import Path
          q=Path('tools/the-files-timeline-v0222.py')
          s=q.read_text(encoding='utf-8')
          old="needle=\"if($script:ActiveSection -eq 'Powers')\"\nif needle not in core:\n    # emit nearby clues for workflow log\n    hits=[line for line in core.splitlines() if 'ActiveSection' in line and ('Powers' in line or 'Story' in line or 'Timeline' in line)]\n    raise SystemExit('Powers renderer hook missing; clues='+repr(hits[:20]))\ncore=core.replace(needle,\"if($script:ActiveSection -eq 'Timeline'){Render-TimelineSection $c $leftPage $rightPage;Update-NavLabels;return}\\n    \"+needle,1)"
          new="needle=\"} elseif($script:CurrentSection -eq 'Powers'){\"\nif needle not in core:\n    hits=[line for line in core.splitlines() if 'CurrentSection' in line and ('Powers' in line or 'Story' in line or 'Timeline' in line)]\n    raise SystemExit('Powers renderer hook missing; clues='+repr(hits[:20]))\ncore=core.replace(needle,\"} elseif($script:CurrentSection -eq 'Timeline'){\\n            Render-TimelineSection $c $leftHost $rightHost\\n        \"+needle,1)"
          if old not in s: raise SystemExit('expected old hook block not found')
          q.write_text(s.replace(old,new,1),encoding='utf-8')
      - name: Build cumulative Timeline release
        shell: pwsh
        run: python tools/the-files-timeline-v0222.py
      - name: Parse launcher and core under Windows PowerShell
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          foreach($f in @('.timeline-v0222-validation/TheFiles.ps1','.timeline-v0222-validation/TheFilesCore.ps1')){
            $tokens=$null;$errors=$null
            [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $f),[ref]$tokens,[ref]$errors)|Out-Null
            if($errors.Count -gt 0){$errors|ForEach-Object{Write-Error (($_.Extent.StartLineNumber.ToString())+': '+$_.Message)};throw "PowerShell parse failed: $f"}
          }
          $p='the-files/timeline-0.2.22-validation.json';$r=Get-Content $p -Raw|ConvertFrom-Json;$r|Add-Member -NotePropertyName powershellParse -NotePropertyValue 'passed-windows-powershell-parser' -Force;$r|ConvertTo-Json -Depth 10|Set-Content $p -Encoding UTF8
      - name: Revalidate package
        shell: python
        run: |
          import json,pathlib,hashlib,base64,gzip
          r=pathlib.Path('.');v=json.loads((r/'the-files/timeline-0.2.22-validation.json').read_text(encoding='utf-8-sig'));b=(r/'the-files'/v['payload']).read_bytes();assert hashlib.sha256(b).hexdigest()==v['payloadSha256'];p=json.loads(b.decode());assert p['version']=='0.2.22';fs={f['path']:f for f in p['files']};assert {'TheFiles.ps1','TheFilesCore.ps1','TheFilesCore.ps1.gz','AppVersion.json'}<=set(fs);raw=base64.b64decode(fs['TheFilesCore.ps1']['contentBase64']);assert gzip.decompress(base64.b64decode(fs['TheFilesCore.ps1.gz']['contentBase64']))==raw;c=raw.decode('utf-8-sig');assert "'Timeline' = @()" in c;assert 'function Render-TimelineSection' in c;assert "CurrentSection -eq 'Timeline'" in c;assert 'Render-PowersSection' in c;assert "'Story' = @(" in c;print('Timeline package validated')
      - name: Commit validated payload and report
        shell: pwsh
        run: |
          git config user.name 'VeyrStudio Update Bot';git config user.email 'actions@users.noreply.github.com'
          git add 'the-files/payload-0.2.22-timeline-builder-part-001.txt' 'the-files/timeline-0.2.22-validation.json';git diff --cached --quiet
          if($LASTEXITCODE -ne 0){git commit -m 'Stage validated The Files v0.2.22 Timeline';git restore 'tools/the-files-timeline-v0222.py';git pull --rebase origin main;git push origin main}
