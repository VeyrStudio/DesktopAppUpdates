# Bundled local tools

The Windows build workflow places the pinned `whisper-cli` executable, its required DLLs, and a static `ffmpeg.exe` in this folder before packaging.

They are bundled inside The Ledger installer. The user does not install them separately.
