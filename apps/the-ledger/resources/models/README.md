# Bundled transcription model

The Windows build workflow downloads the balanced English `ggml-small.en-q5_1.bin` model from the official whisper.cpp model repository and places it here before packaging.

The model is bundled inside The Ledger installer. The user does not download it separately.
