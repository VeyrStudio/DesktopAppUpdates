<Application x:Class="TheTome.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <SolidColorBrush x:Key="Gold" Color="#D6B06A"/>
        <SolidColorBrush x:Key="Ivory" Color="#F0E7D6"/>
        <SolidColorBrush x:Key="Panel" Color="#26152F"/>
        <SolidColorBrush x:Key="Panel2" Color="#1B1022"/>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Ivory}"/>
            <Setter Property="Background" Value="#4A285E"/>
            <Setter Property="BorderBrush" Value="#8F6D48"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Cormorant Garamond, Georgia"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#140C19"/>
            <Setter Property="Foreground" Value="{StaticResource Ivory}"/>
            <Setter Property="BorderBrush" Value="#6E526F"/>
            <Setter Property="CaretBrush" Value="{StaticResource Ivory}"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#21142A"/>
            <Setter Property="Foreground" Value="#1B1022"/>
            <Setter Property="Padding" Value="7,5"/>
        </Style>
    </Application.Resources>
</Application>